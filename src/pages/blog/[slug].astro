---
import Layout from "../../layouts/Layout.astro";
import Navbar from "../../components/Navbar.astro";
import Footer from "../../components/Footer.astro";
import { type Lang, langPrefix } from "../../i18n/translations";
import { articles } from "../../data/articles";
import { landingPages } from "../../data/landing";

interface Props { lang?: Lang }
const lang = Astro.props.lang || "uz" as Lang;
const prefix = langPrefix(lang);

const { slug } = Astro.params;
const article = articles.find((a) => a.slug === slug);

if (!article) {
  return Astro.redirect(prefix + "/blog");
}

const dateObj = new Date(article.date);
const dateStr = dateObj.toLocaleDateString(lang === "uz" ? "uz-UZ" : "ru-RU", {
  year: "numeric",
  month: "long",
  day: "numeric",
});

const updatedStr = article.updated
  ? new Date(article.updated).toLocaleDateString(lang === "uz" ? "uz-UZ" : "ru-RU", {
      year: "numeric",
      month: "long",
      day: "numeric",
    })
  : null;

/* Har bir maqolani mos landing sahifalarga bog'laymiz — ichki link tarmog'i */
const relatedMap: Record<string, string[]> = {
  "telegram-kanalni-pullik-qilish": ["getolog-nima", "telegram-kanalga-tolov-qabul-qilish", "telegram-obuna-bot"],
  "obuna-bot-sozlash": ["telegram-obuna-bot", "getolog-nima", "obuna-tugaganda-kanaldan-chiqarish"],
  "pullik-kanal-uchun-maslahatlar": ["obuna-tugaganda-kanaldan-chiqarish", "telegram-obuna-bot", "getolog-nima"],
  "invite-link-xavfsizligi": ["obuna-tugaganda-kanaldan-chiqarish", "telegram-obuna-bot", "telegram-kanalga-tolov-qabul-qilish"],
};

const relatedLanding = (relatedMap[article.slug] || [])
  .map((s) => landingPages.find((p) => p.slug === s))
  .filter((p): p is NonNullable<typeof p> => Boolean(p));

const backText = { uz: "Barcha maqolalar", ru: "Все статьи" };
const readTimeLabel = { uz: "O'qish vaqti", ru: "Время чтения" };
const updatedLabel = { uz: "Yangilangan", ru: "Обновлено" };
const relatedTitle = { uz: "Shuni ham o'qing", ru: "Читайте также" };
const faqTitle = { uz: "Ko'p so'raladigan savollar", ru: "Часто задаваемые вопросы" };

const articleUrl = `https://getolog.uz${prefix}/blog/${slug}`;
const blogUrl = `https://getolog.uz${prefix}/blog`;
const homeUrl = lang === "uz" ? "https://getolog.uz" : "https://getolog.uz/ru";
const blogLabel = { uz: "Maqolalar", ru: "Статьи" };

const schemaData = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Article",
      "@id": articleUrl + "#article",
      "headline": article.title[lang],
      "description": article.description[lang],
      "datePublished": article.date,
      "dateModified": article.updated || article.date,
      "image": "https://getolog.uz/logo.png",
      "author": { "@type": "Organization", "name": "Getolog", "url": "https://getolog.uz" },
      "publisher": { "@id": "https://getolog.uz/#organization" },
      "mainEntityOfPage": { "@type": "WebPage", "@id": articleUrl },
      "inLanguage": lang,
      "about": { "@id": "https://getolog.uz/#software" },
      "articleSection": article.category[lang],
    },
    {
      "@type": "BreadcrumbList",
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Getolog", "item": homeUrl },
        { "@type": "ListItem", "position": 2, "name": blogLabel[lang], "item": blogUrl },
        { "@type": "ListItem", "position": 3, "name": article.title[lang], "item": articleUrl },
      ],
    },
    ...(article.faq.length > 0 ? [{
      "@type": "FAQPage",
      "mainEntity": article.faq.map((f) => ({
        "@type": "Question",
        "name": f.q[lang],
        "acceptedAnswer": { "@type": "Answer", "text": f.a[lang] },
      })),
    }] : []),
  ],
};

export function getStaticPaths() {
  return articles.map((a) => ({ params: { slug: a.slug } }));
}
---

<Layout title={`${article.title[lang]} — Getolog`} description={article.description[lang]} lang={lang} ogType="article">
  <Navbar lang={lang} />

  <script type="application/ld+json" set:html={JSON.stringify(schemaData)} />

  <article class="article-page">
    <div class="container">
      <nav class="article-breadcrumb" aria-label="Breadcrumb">
        <a href={homeUrl}>Getolog</a>
        <span>/</span>
        <a href={`${prefix}/blog`}>{blogLabel[lang]}</a>
        <span>/</span>
        <span class="current">{article.title[lang]}</span>
      </nav>

      <header class="article-header">
        <div class="article-meta">
          <span class="article-category">{article.category[lang]}</span>
          <span class="article-date">{dateStr}</span>
          {updatedStr && <span class="article-date">{updatedLabel[lang]}: {updatedStr}</span>}
          <span class="article-read">{readTimeLabel[lang]}: {article.readTime[lang]}</span>
        </div>
        <h1 class="article-title">{article.title[lang]}</h1>
        <p class="article-desc">{article.description[lang]}</p>
      </header>

      <div class="article-body" set:html={article.html[lang]} />

      {article.faq.length > 0 && (
        <section class="article-faq">
          <h2>{faqTitle[lang]}</h2>
          <div class="faq-list">
            {article.faq.map((f) => (
              <details class="faq-item">
                <summary class="faq-question">{f.q[lang]}</summary>
                <p class="faq-answer">{f.a[lang]}</p>
              </details>
            ))}
          </div>
        </section>
      )}

      {relatedLanding.length > 0 && (
        <section class="article-related">
          <h2>{relatedTitle[lang]}</h2>
          <ul>
            {relatedLanding.map((p) => (
              <li><a href={`${prefix}/${p.slug}`}>{p.h1[lang]}</a></li>
            ))}
          </ul>
        </section>
      )}

      <div class="article-cta">
        <p class="article-cta__text">
          {lang === "uz"
            ? "Telegram kanalingizni avtomatlashtirishga tayyormisiz?"
            : "Готовы автоматизировать свой Telegram-канал?"}
        </p>
        <a href="https://t.me/getologbot" target="_blank" rel="noopener noreferrer" class="btn btn--primary btn--lg">
          {lang === "uz" ? "Boshlash" : "Начать"}
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
        <p class="article-cta__help">
          {lang === "uz" ? "Savolingiz bormi? Support: " : "Есть вопрос? Поддержка: "}
          <a href="https://t.me/getolog_bot" target="_blank" rel="noopener noreferrer">@getolog_bot</a>
        </p>
      </div>
    </div>
  </article>

  <Footer lang={lang} />
</Layout>

<style>
  .article-page {
    padding-top: 120px;
    padding-bottom: 80px;
  }

  .article-breadcrumb {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 32px;
    font-size: 0.85rem;
    flex-wrap: wrap;
  }

  .article-breadcrumb a {
    color: var(--accent);
    font-weight: 500;
    transition: all var(--transition);
  }

  .article-breadcrumb a:hover {
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .article-breadcrumb span {
    color: var(--light-400);
    opacity: 0.5;
  }

  .article-breadcrumb .current {
    color: var(--light-400);
    opacity: 0.7;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    max-width: 300px;
  }

  .article-header {
    text-align: center;
    max-width: 720px;
    margin: 0 auto 48px;
    padding-bottom: 40px;
    border-bottom: 1px solid rgba(26, 29, 34, 0.06);
  }

  .article-meta {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16px;
    margin-bottom: 20px;
    flex-wrap: wrap;
  }

  .article-category {
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--accent);
    background: rgba(91, 107, 122, 0.1);
    padding: 4px 12px;
    border-radius: 100px;
  }

  .article-date,
  .article-read {
    font-size: 0.85rem;
    color: var(--light-400);
  }

  .article-title {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 700;
    margin-bottom: 16px;
    letter-spacing: -0.5px;
    line-height: 1.25;
  }

  .article-desc {
    font-size: 1.1rem;
    color: var(--light-400);
    line-height: 1.6;
  }

  .article-body {
    max-width: 720px;
    margin: 0 auto;
  }

  .article-body :global(h2) {
    font-size: 1.4rem;
    font-weight: 700;
    margin-top: 40px;
    margin-bottom: 16px;
    color: var(--white);
  }

  .article-body :global(h3) {
    font-size: 1.1rem;
    font-weight: 600;
    margin-top: 28px;
    margin-bottom: 12px;
    color: var(--light);
  }

  .article-body :global(p) {
    font-size: 0.95rem;
    color: var(--light-400);
    line-height: 1.8;
    margin-bottom: 16px;
  }

  .article-body :global(ul),
  .article-body :global(ol) {
    padding-left: 20px;
    margin-bottom: 16px;
  }

  .article-body :global(li) {
    font-size: 0.95rem;
    color: var(--light-400);
    line-height: 1.7;
    margin-bottom: 8px;
  }

  .article-body :global(li strong) {
    color: var(--light);
  }

  .article-body :global(code) {
    background: rgba(91, 107, 122, 0.1);
    padding: 2px 8px;
    border-radius: 4px;
    font-size: 0.9em;
    color: var(--accent);
  }

  .article-cta {
    max-width: 720px;
    margin: 56px auto 0;
    padding-top: 40px;
    border-top: 1px solid rgba(26, 29, 34, 0.06);
    text-align: center;
  }

  .article-cta__text {
    font-size: 1.2rem;
    font-weight: 600;
    color: var(--white);
    margin-bottom: 24px;
  }

  .article-cta__help {
    margin-top: 18px;
    font-size: 0.88rem;
    color: var(--light-400);
  }

  .article-cta__help a {
    color: var(--accent);
    font-weight: 500;
  }

  .article-related {
    max-width: 720px;
    margin: 48px auto 0;
    padding-top: 40px;
    border-top: 1px solid rgba(26, 29, 34, 0.06);
  }

  .article-related h2 {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 20px;
  }

  .article-related ul {
    list-style: none;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .article-related a {
    color: var(--accent);
    font-weight: 500;
    font-size: 0.95rem;
  }

  .article-related a:hover {
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  @media (max-width: 768px) {
    .article-page {
      padding-top: 100px;
      padding-bottom: 60px;
    }

    .article-header {
      margin-bottom: 36px;
      padding-bottom: 32px;
    }

    .article-title {
      font-size: 1.5rem;
    }

    .article-desc {
      font-size: 0.95rem;
    }

    .article-body :global(h2) {
      font-size: 1.25rem;
      margin-top: 32px;
    }

    .article-cta {
      margin-top: 40px;
      padding-top: 32px;
    }

    .article-cta__text {
      font-size: 1.05rem;
    }

    .article-faq h2 {
      font-size: 1.25rem;
    }
  }

  /* FAQ */
  .article-faq {
    max-width: 720px;
    margin: 48px auto 0;
    padding-top: 40px;
    border-top: 1px solid rgba(26, 29, 34, 0.06);
  }

  .article-faq h2 {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 24px;
  }

  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .faq-item {
    background: var(--dark-800);
    border: 1px solid rgba(26, 29, 34, 0.06);
    border-radius: var(--radius);
    overflow: hidden;
    transition: border-color var(--transition);
  }

  .faq-item[open] {
    border-color: rgba(91, 107, 122, 0.2);
  }

  .faq-question {
    padding: 18px 24px;
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--light);
    cursor: pointer;
    list-style: none;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
  }

  .faq-question::-webkit-details-marker {
    display: none;
  }

  .faq-question::after {
    content: '+';
    font-size: 1.3rem;
    font-weight: 400;
    color: var(--accent);
    flex-shrink: 0;
    transition: transform var(--transition);
  }

  .faq-item[open] .faq-question::after {
    content: '\2212';
  }

  .faq-answer {
    padding: 0 24px 18px;
    font-size: 0.9rem;
    color: var(--light-400);
    line-height: 1.7;
    margin: 0;
  }
</style>
